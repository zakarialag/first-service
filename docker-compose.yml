version: '3.7'

services:
  first-server:
    build:
      context: ./
      dockerfile: Dockerfile
    image: first-server:latest
    container_name: first-server
    ports:
      - "8089:8089"
    restart: unless-stopped
    network_mode: host
